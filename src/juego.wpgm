import wollok.game.*
import enemigos.*
import trampas.*
import config.*

program game {
	game.title("Plantas vs Zombies")
	game.boardGround("grass03.png")
	game.width(20)
	game.height(10)
	game.addVisual(plantita)
	game.start()
}