import wollok.game.*
import enemigos.*
import trampas.*
import config.*
import objetosBasicos.*

program game {
	game.title("Plantas vs Zombies")
	game.boardGround("grass03.png")
	game.width(20)
	game.height(10)
	config.configurarTeclas()
	config.configurarAcciones()
	config.agregarTorre()

	game.addVisualCharacter(zombie)
	game.start()
}