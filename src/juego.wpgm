import wollok.game.*
import config.*

program game {
	game.title("Plantas vs Zombies")
	game.boardGround("tablero.png")	
	game.width(20)
	game.height(10)
	config.iniciar()
	game.start()
}

