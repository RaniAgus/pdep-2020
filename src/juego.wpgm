import wollok.game.*
import config.*

program game {
	game.title("Plantas vs Zombies")
	game.boardGround("grass03.png")	
	game.width(20)
	game.height(10)
	config.iniciar()
	game.start()
}

