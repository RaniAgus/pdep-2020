import wollok.game.*
import config.*
import sonidos.*


program game {
	game.title("Plantas vs Zombies")
	game.boardGround("tablero.png")	
	game.width(22)
	game.height(10)
	config.iniciar()
	game.start()
	
}

